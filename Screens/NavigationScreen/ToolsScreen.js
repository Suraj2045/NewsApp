import * as React from 'react';
import { View, Text } from 'react-native';
import Ilovepdf from '../../common/webview/Ilovepdf';
import Stopnavigation from '../../common/webview/stopnavigation'


export default function ToolsScreen({ navigation }) {
    return (
        <Ilovepdf/>
    );
}